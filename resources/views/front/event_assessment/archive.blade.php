@extends('front.layout.app')
@section('content')
    <main>
        <div class="container-fluid px-4">
            <h2 class="mt-4 main_heading">Archive Event Assessments</h2>
            <hr class="w-50 mx-auto">


            <div class="card mb-4">
                <div class="card-body">
                    <table id="datatablesSimple">
                        <thead>
                            <tr>
                                <th>S.no</th>
                                <th>Event</th>
                                <th>Assessment</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @php
                                $key = 1;
                            @endphp
                            @foreach ($event_assessments as $event_assessment)
                                <tr>
                                    <td>{{ $key }}</td>
                                    <td>{{ $event_assessment->event->Name ?? '' }}</td>
                                    <td>{{ $event_assessment->assessment->Name ?? '' }}</td>
                                    <td>
                                        <div class="d-flex">
                                            <a href="{{ route('event_assessment.toggle.archive', ['id' => $event_assessment->id]) }}"
                                                class="btn btn-success text-white" title="unarchive">
                                                <i class="fa fa-clock-rotate-left"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                                @php
                                    $key++;
                                @endphp
                            @endforeach
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
@endsection
